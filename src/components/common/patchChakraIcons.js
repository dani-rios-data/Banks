// Este archivo exporta React.forwardRef para que esté disponible
// cuando se importe en componentes que lo necesiten
import React from 'react';

// Exportamos forwardRef desde React
export const forwardRef = React.forwardRef; 